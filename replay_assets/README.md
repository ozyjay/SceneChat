# Replay assets

The committed asset is a synthetic SVG illustration with prepared, normalised detector annotations and scene responses. It contains no visitor image. Add future scenarios to `manifest.json`; keep assets local and ensure their licensing permits public display.

