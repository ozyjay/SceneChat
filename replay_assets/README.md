# Replay assets

The committed asset is a synthetic illustration with prepared, normalised detector annotations and scene responses. The manifest uses the raster PNG so the same prepared frame is accepted by vision-model routes; the SVG is retained as its editable source. Neither contains a visitor image. Add future scenarios to `manifest.json`; keep assets local and ensure their licensing permits public display.
